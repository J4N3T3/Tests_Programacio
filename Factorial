#include <stdio.h>

int main() {
    int num1, i, fact;
    fact = 1;
    printf("Escriu un numero:\n");
    scanf_s("%d", &num1);
    for (i = 1; i <= num1; i++) {
        fact = fact*i;
    }
    printf("%d", fact);
}
